import React from 'react';

const styleType = {
    color: 'blue',
    backgroundColor: 'red',
};

const Colors = () => {
    return (
        <h1 style={ styleType }>The word is: hello</h1>
    );
};

export default Colors;